/**
 * Weekly report cron
 *
 * Checks every hour. On Monday between 07:00–09:00 UTC, generates
 * weekly reports for all active users who don't already have one.
 */
export declare function scheduleWeeklyReport(): void;
